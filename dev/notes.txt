
Breeze_G4x4_T8th_D4th_S3 (all faces)(uncustom texture), Breeze_G4x4_T8th_D4th_S3 (wip)(all faces)(uncustom texture), Breeze_G4x4_T16th_D4th_S3 (all faces)(uncustom texture) all have slight UV errors around the center hole of each cluster of 5 holes.
useless tags (just rename the shape):
  Ud = upside-down
  Iv = inverted
  Np = non-prismatic
useless tags (just use multiple shape names):
  Bw = basketweave
potential tags:
  Is = inset
  Fl = frameless
  for both of the above tags:
    can come before the shape name(s) to affect only the outer frame
    can come after any shape name to affect the shape
    Can come after an underscore after the shape names to affect every shape individually

Da = Depth amount, Db = depth binary, Dc = Depth centered amount.

universal format of numbers:
  [num"p"][num"n"][num"d"]
  at least one section must be used.
  p is for pixels
  n is for numerator
  d is for denominator
  when denominator is used without numerator, numerator is 1.
  when numerator is used without denominator, denominator is the smallest power of 10 greater than the numerator.
  examples:
    1p = 1
    1p2n = 1.2
    1p2n3d = 1.66
    2n3d = 0.66
    3d = 0.33
    1p3d = 1.33
    
Thickness specifiers:
  T = frame and big details and little details
  F = frame
  D = big details and little details
  B = big details
  L = little details
    
Wood_Tropicalwood_Breeze_G1x1F6pB5pL4pSameNearI1pNearI1pSame

-----------------------------

Breeze G<1x1|2x2|1x2|2x1|2x4|4x2|4x4> T<8|16>th 

----------------------------------------------------------------------------

names:
  coal / soot / lump / mote / mite
  veer / wave / hook / loop / cane / wake / lake
  span
  sake
  post / moat / root / tree
  seer / wary (two eyes)
  bean
  rain
  snow (4-pointed snowflake)
  

notes that are helpful for planning:
  resource types may be defined at the bottom of an asset json.
  the sound set for Rock is called "Stone"
  As long as the mod continues to provide asset jsons for deprecated block IDs, it can also specify in those jsons that the deprecated blocks drop the newer blocks.
  on windows, The "date created" tag persists when a file is deleted and another one with the same name is created, which makes it not very useful.

==============================================================================

notes on UVs:
  outside sides are sideways compared to others:
    dice
    G4x4T1pVoid
    G4x4T2pVoid
other notes:
  hytale update 4 added new state data to smooth stone brick slabs to make a double slab drop 2 slabs when broken.
  why is bronze in the basalt family?

atlas.py: atlas interactive prompt:
  while hovering over cell with link:
    [u] unlink
    [r] rename //renames the tile file and updates the link.
    [del] delete //deletes the tile file. does NOT remove the link.
    [s] show //show the tile file
  while hovering over cell with or without link:
    [e] export now / [i] import now // TODO: if there is no link, prompt whether user really wants to create one, then prompt for file name.
    [backspace] wipe // delete the contents of this cell.
    [m] move //moves cell contents and link if present. cannot move to cell that already has a link.
  while hovering over cell without link:
    [l] link //uses a new menu that allows user to see which tile files are part of links and click an unlinked file.
  at any time:
    [q] quit without saving
    [enter] save and exit


TODO:

move color shelf into a data folder.

atlas.py:
  allow target directory to be specified by an argument.
  import utilities.
  interactive mode:
    Add keyboard shortcuts from above design.
    make newline acceptable in any text.
    add info to tile show feature.
  in tile name prompt, make rules for whether or not a tile name will be accepted injectable.
  give tile name prompt a prompt_definition makeover.
  padding around text.
  
use hytale stock translations of materials in translated names of breeze blocks.
make ${set} more regular
don't use ${set} for families that shouldn't have a set.
texture generation:
  add a smooth stone brick texture simplifier.
  add a front texture generator that uses the thumbnail mask to create shading.

factor language out of generate.py.

atlas.py health:
  make saving of config file better.
    split config into config settings and config mappings.
  refactor do_tile_import to not fall out of such a big if.
  replace discover and organize with values of a single enum, and maybe rename them to something that makes more sense.
  add procedure tests.
  replace tkinter with pygame everywhere.
  fix tile_name_to_path only being used in some places.
  
add rename detection to atlas.py.
make a model variants system.
  idea: follow the depth information (Db1000) with "_WoodOnly" or "_StoneOnly" so that suffix must be removed and then generate.py continues as normal.
language:
  translate to every language in the hytale multi-language bundle mod.
  "cobogo" in Portuguese.
  synonym fuzzing - if any word in the pool can make a round-trip translation to any other word in the pool, use the foreign word that is the step between those two words.
make option to rebuild destination mod.
add a command to atlas.py to predict which tiles would be changed upon export or import, and optionally, to show the changes.
add terracotta back:
  don't add clay until hytale's method of obtaining hardened clay is decided.
make string library, add length assertions back to remove_prefix etc.
add all models from designs canvas.
improve texture shading on all models.
add THIN cloth breeze blocks (basically lace/drapes with holes)
Make system for changing the depth of breeze blocks.
outer corner breeze blocks.
inner corner breeze blocks (G2x2 and higher only).
Add breeze blocks for wood types and stone types from other popular mods.
Add pattern "None".
diagonal breeze blocks.
curved breeze blocks.
optimize models.
patterns bigger than a single block.
make a breeze mason's bench that can make weirder blocks with other depth modes.
create ornate versions of some models.
document what is a family, what is a family suffix, what is a family prefix, and reference assets from base game.
fix bug where things are held in the wrong position.
PNG optimization
  pngrewrite before pngout (requires test to see if it's any smaller when used with pngout).
  

shelved:
add command line args to renamer.
what is creative build light made out of?
create JSON recipe input ItemID control.


cancelled:
pickling as a context manager. (this already exists: import shelve)


done:
upgrade renamer.py to optionally edit the content of asset definition files (or any other files in a second directory) too.
upgrade generate.py to support all woods.
make a thumbnail generation system.
upgrade generate.py to support stone
add directory tree to the top of renamer
rename thicknesses from meter fractions to pixel counts.
upgrade generate.py to support terracotta.
thicken G2x2 Nope.
thicken SlowNeckNeckSlow.
add default values to renamer.
rename G2x2T2pHead to G2x2T2pL1pHead or fix model.
change naming convention so that frame thickness refers to the thickness of frame present within a single breeze block.
complete pre-release checklist.
fix G2x2pT2pHead to match its name because it currently DOES NOT. Also fix the names of whichever ones have red cloth above them.
adjust crafting count
pre-release checklist:
  -No obvious texture glitches. - OK
  -everything looks like Hytale. - OK
  -plays well in survival. - OK
  -I will never want to rename any of these blocks. - OK
  -mod has no unnecessary files. - OK
publish mod.
create a particle color system.
  hytale constants module for python.
  3d median.
upgrade thumbnail generator to invert background when dark textures are detected.
parse string to match format of structure full of strings
fix names of white sandstone, red sandstone, peach, and ledge breeze blocks.
parsing:
  add an option to flatten result (actually separate method)
clean up model name parsing with default values for max integer length.
create mismatch.py
rework atlas.py organize window so that it only returns when it meets an objective which can be specified as an argument.
libretranslate
improve name translation by splitting, translating parts, and recombining.
translate to more languages.
add translation corrections.
texture issues:
  fix texture errors on the back of G4x4T4pVoid. - fixed
  back of G2x2F2pB4pL2pHead. - overlap fixed, uv fixed
  back of G2x2T2pHead. - fixed overlap, no uv issues.
fixed back uv of dice
fixed all sides of G1x1T4pHead. coiuld be simplified.
fixed all side uvs on G2x2F2pB4pL2pHead. could be simplified.
fixed back UVs on G4x4T1pVoid
fix all side texture directions on all models, except for those which remain sideways but still look correct.
add sparks when landing on gold breeze block (to match solid gold block).
add bronze, zinc, copper, and iron.
add materials from Hytale update 4.
replace scandir with listdir
atlas.py:
  Add command line arg for interactive mode.
  separate PromptResponseType into TilePromptResponse and AtlasPromptResponse.
improve thumbnails
atlas.py health:
  add warnings when importing and a tile in the config does not exist as a file.
  do the int vector refactor.
  add detection for out-of-bounds coordinates while importing (Currently it just draws off the edge of the atlas with no error).
  make config saving better.
    make config data a class.
make prompt_definition a data class of some kind instead of a dict.
atlas.py interactive mode:
  minimum window height.
rename "view" to "show" in atlas commands.
png optimization:
  async up to 6 subprocess.
factor parsing out of generate.py.
atlas.py interactive mode:
  make tooltip adapt to which keys are valid by looking up key descriptions.